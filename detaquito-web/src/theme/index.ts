import { dark, light, gray, primary, shadow, error, breakpoints, Theme } from "./Theme";
import type { ThemeType } from "./Theme";

export { Theme, dark, light, gray, primary, error, shadow, breakpoints };
export type { ThemeType };
